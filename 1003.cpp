#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    int T, N;
    cin >> T;
    vector<int> testcase(T);

    for(int i=0; i<T; i++){
        cin >> N;
        testcase[i]=N;
    }

    
    return 0;
}